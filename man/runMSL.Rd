% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scLearn_model_learning.R
\name{runMSL}
\alias{runMSL}
\title{Multi Similarity Loss-Based Metric Learning for Single-Cell Data}
\usage{
runMSL(
  high_varGenes,
  expression_profile,
  sample_information,
  alpha = 2,
  beta = 50,
  margin = 0.1,
  lambda = 1e-04,
  learn_rate = 1e-05,
  max_iter = 200,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{high_varGenes}{Character vector of highly variable gene names.}

\item{expression_profile}{A numeric matrix of gene expression values (genes x cells).}

\item{sample_information}{A named vector of cell type labels (names match column names of expression_profile).}

\item{alpha}{Scaling parameter for positive similarities (default: 2.0).}

\item{beta}{Scaling parameter for negative similarities (default: 50.0).}

\item{margin}{Margin threshold to filter informative pairs (default: 0.1).}

\item{lambda}{L2 regularization strength (default: 1e-4).}

\item{learn_rate}{Learning rate for gradient descent (default: 1e-5).}

\item{max_iter}{Maximum number of optimization iterations (default: 200).}

\item{seed}{Random seed (default: 1).}

\item{verbose}{Logical; whether to print progress information (default: TRUE).}
}
\value{
A list containing:
\itemize{
  \item \code{expression_profile_trans}: Transformed expression matrix (genes x cells).
  \item \code{expression_profile_origin}: Original filtered expression matrix (genes x cells).
  \item \code{trans_matrix}: Learned transformation matrix (L).
  \item \code{sample_information}: Sorted cell type labels.
}
}
\description{
Implements a metric learning algorithm based on Multi Similarity Loss (MSL),
as an alternative to Triplet Loss in LMNN. It enhances learning from limited
sample pairs by considering multiple similarity cues and penalizes overfitting
through L2 regularization. Optimized for single-cell expression data.
}
